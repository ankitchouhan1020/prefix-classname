{
  "devDependencies": {
    "@types/jscodeshift": "^0.11.0",
    "jscodeshift": "^0.11.0"
  },
  "scripts": {
    "test": "jest",
    "check:types": "tsc --noEmit",
    "check:formatting": "yarn prettier --check \"transforms/**\""
  },
  "dependencies": {
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.3",
    "typescript": "^4.2.3"
  }
}
